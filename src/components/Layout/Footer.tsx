import { Layout } from 'antd';

const { Footer: LayoutFooter } = Layout;

const Footer = () => (
  <LayoutFooter className="footer_main">
    <div className="footer_logo">Logo</div>
  </LayoutFooter>
)

export default Footer;